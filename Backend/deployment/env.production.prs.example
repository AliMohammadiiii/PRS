# PRS Production Environment Configuration
# Copy this file to /opt/prs/.env and update with your actual values
# NEVER commit the real .env file to version control

# Django Settings
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-base64-32
DEBUG=False
# Note: ALLOWED_HOSTS can also be set via DJANGO_ALLOWED_HOSTS in the systemd service file
ALLOWED_HOSTS=innovation.nntc.io,www.innovation.nntc.io,127.0.0.1,localhost

# Database Configuration (PostgreSQL recommended for production)
DATABASE_URL=postgresql://prs:your-database-password@localhost:5432/prs

# Static and Media Files
STATIC_ROOT=/opt/prs/Backend/staticfiles
MEDIA_ROOT=/opt/prs/media

# Subpath deployment: PRS is served at https://innovation.nntc.io/PRS/
FORCE_SCRIPT_NAME=/PRS

# CORS Configuration (comma-separated list of allowed origins)
# Adjust if you serve PRS from additional domains or protocols
CORS_ALLOWED_ORIGINS=https://innovation.nntc.io,https://www.innovation.nntc.io

# Logging
LOG_FILE=/var/log/prs/django.log
LOG_LEVEL=INFO

# Security
# Set to False to allow both HTTP and HTTPS, True to force HTTPS redirect
SECURE_SSL_REDIRECT=True

# Email Configuration (optional - for notifications)
# For production, use SMTP backend
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-password
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False
# DEFAULT_FROM_EMAIL=noreply@innovation.nntc.io

# PRS Completion Email (organizational inbox for completed purchase requests)
# PRS_COMPLETION_EMAIL=finance@example.com




